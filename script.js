//My changes aren't saved i dont now hwy
let display=0;
let valueA= 0;
let valueB= 0;
let operator=0;
let firtsValue = false;

//Here the functions
function resolve(input){
    valueA = input;
}

function operatorgetter(opType){
		valueA = input;
		operator = opType;
		display = valueA;
		firstValue = true;
}

function equalAction{
	switch (operator) {
		case(sum):
		case()
		
	}
}

