//My changes aren't saved i dont now hwy
let display=0;
let valueA= 0;
let valueB= 0;
let answer=0;
let operator=0;
let firtsValue = false;

//Here the functions
function resolve(input){
    valueA = input;
}

//<<******Code for using the UI buttons*********>>

//First Value setter

function setValueA(inputUser){
	//here i must place the inputUser in the display and make a string of numbers there
}

function operatorgetter(opType){
		//I need to make a decision tree for the stupid user
		//Transform string into number with decimal
		valueA = input;
		operator = opType;
		display = valueA;
		firstValue = true;
}

if(firstValue){
	
}

function equalAction(){
	switch (operator) {
		case 0:
			answer = valueA+valueB;
			break;
		case 1:
			answer = valueA-valueB;
			break;
		case 2:
			answer = valueA*valueB;
			break;
		case 3:
			answer = valueA/valueB;
			break;
		default:
			
		
	}
}

